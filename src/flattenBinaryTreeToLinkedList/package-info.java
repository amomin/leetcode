/**
 * 
 */
/** <h1> Flatten Binary tree </h1>
 * 
 * <p> Turn a binary tree into a linear tree where only 
 * left children are always null </p>
 * 
 * <p> Just do this recursively on the children, and
 * then stack the left child in between the root and 
 * the right child. </p>
 * @author user
 *
 */
package flattenBinaryTreeToLinkedList;